# @defaultSensitive=inferFromPrefix(PUBLIC_)
# @defaultRequired=infer
# @generateTypes(lang=ts, path=src/lib/convex/convex-env.d.ts)
# @redactLogs=true
# @preventLeaks=true
# ---

# ====================================
# Better Auth
# ====================================

# Secret for signing sessions and tokens
# @required @sensitive
BETTER_AUTH_SECRET=

# Base URL for OAuth redirects and deep links
# @type=url @required
SITE_URL=

# ====================================
# Email System (Resend)
# ====================================

# Resend API key for transactional email delivery
# @required @sensitive
RESEND_API_KEY=

# Sender email address for auth emails
# @required
AUTH_EMAIL=

# Production URL for email assets (logo images, footer links)
# Must always be a public domain (email clients cannot access localhost)
# @type=url @required
EMAIL_ASSET_URL=

# Resend webhook signing secret
# @optional @sensitive
RESEND_WEBHOOK_SECRET=

# ====================================
# Paddle Billing
# ====================================

# Paddle API Key (server-side, for billing portal sessions)
# Get from: Paddle Dashboard → Developer Tools → Authentication
#
# FACULTATIVES TANT QU'AUCUN ÉCRAN NE VEND. Le backend de facturation existe,
# aucune interface ne l'appelle : exiger ces clés bloquait chaque déploiement
# pour une fonctionnalité qui n'est pas encore livrée. Elles redeviendront
# obligatoires le jour où l'abonnement sera branché — et ce jour-là, l'oubli
# doit bien faire échouer le déploiement.
# @optional @sensitive
PADDLE_API_KEY=

# Paddle Webhook Secret (for verifying webhook signatures)
# Get from: Paddle Dashboard → Notifications → Webhook Secret
# @optional @sensitive
PADDLE_WEBHOOK_SECRET=

# Identifiants de prix Paddle — SIX, deux offres par trois paliers de taille.
#
# Le business plan (document 03) vend le MÊME produit à trois prix selon la
# taille de la cantine, parce que c'est la valeur qui change et non la
# prestation. Chez Paddle, cela se modélise par deux produits et trois prix
# chacun. Le palier est calculé côté serveur à partir de `couvertsJour` — voir
# `palierDeTaille` dans `billing.ts` — et jamais transmis par le client, qu'un
# navigateur pourrait falsifier pour payer le tarif d'en dessous.
#
#   S  moins de 250 couverts/jour   690 € le bilan, 190 €/mois
#   M  de 250 à 800                1 190 € le bilan, 290 €/mois
#   L  au-delà de 800              1 900 € le bilan, 390 €/mois
#
# L'étage « opérateur » a été supprimé : voir l'en-tête de `billing.ts`.
# Get from: Paddle Dashboard → Catalog → Prices
# @optional
PADDLE_PRICE_DIAGNOSTIC_S=

# @optional
PADDLE_PRICE_DIAGNOSTIC_M=

# @optional
PADDLE_PRICE_DIAGNOSTIC_L=

# @optional
PADDLE_PRICE_CONFORMITE_S=

# @optional
PADDLE_PRICE_CONFORMITE_M=

# @optional
PADDLE_PRICE_CONFORMITE_L=

# ====================================
# Claude — extraction et classification
# ====================================

# Cle API Anthropic, lue par le SDK depuis l'environnement.
#
# ELLE EST LA SEULE VARIABLE SANS LAQUELLE LE PRODUIT NE FAIT RIEN : ni lecture
# de facture, ni classification. Le schema exigeait jusqu ici OPENROUTER_API_KEY,
# heritee d un chat de support qui n existe plus, et ne mentionnait pas celle-ci.
# Le deploiement reclamait donc une cle que rien n utilise, et laissait passer
# l absence de celle dont tout depend : un deploiement reussi aurait echoue a la
# premiere facture.
# @required @sensitive
ANTHROPIC_API_KEY=

# ====================================
# OAuth Providers
# ====================================

# Google OAuth
# @optional
AUTH_GOOGLE_ID=

# @optional @sensitive
AUTH_GOOGLE_SECRET=

# GitHub OAuth
# @optional
AUTH_GITHUB_ID=

# @optional @sensitive
AUTH_GITHUB_SECRET=

# ====================================
# E2E Testing
# ====================================

# Secret for test mutations (never set in production)
# @optional @sensitive
AUTH_E2E_TEST_SECRET=

# ====================================
# Preview Dev (auto-seeded on preview deployments)
# ====================================

# Password for the auto-seeded preview admin (admin@preview.dev)
# Only set on preview Convex deployments by deploy.ts
# @optional @sensitive
PREVIEW_ADMIN_PASSWORD=

# ====================================
# Local Dev (auto-generated by vite.config.ts)
# ====================================

# Flag indicating local Convex dev mode
# @type=boolean @optional
LOCAL_CONVEX_DEV=

# Seeded admin credentials for local dev
# @optional
LOCAL_SEEDED_ADMIN_EMAIL=

# @optional @sensitive
LOCAL_SEEDED_ADMIN_PASSWORD=

# @optional
LOCAL_SEEDED_ADMIN_NAME=
