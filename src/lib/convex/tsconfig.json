{
	/* This TypeScript project config describes the environment that
	 * Convex functions run in and is used to typecheck them.
	 * You can modify it, but some settings required to use Convex.
	 */
	"compilerOptions": {
		/* These settings are not required by Convex and can be modified. */
		"allowJs": true,
		"strict": true,
		// Aligné sur le tsconfig de l'app : sans lui, `check:convex` passe au vert
		// sur des accès indexés non vérifiés que `check` refuse — et le backend
		// dérive silencieusement vers une strictesse moindre que le reste.
		"noUncheckedIndexedAccess": true,
		"moduleResolution": "Bundler",
		"jsx": "react-jsx",
		"skipLibCheck": true,
		"allowSyntheticDefaultImports": true,
		"resolveJsonModule": true,

		/* TypeScript 6 changed the default of `types` from "all installed" to []
		 * — Convex functions reference `process.env.*`, so we need to opt back
		 * into the node types explicitly. */
		"types": ["node"],

		/* These compiler options are required by Convex */
		"target": "ESNext",
		"lib": ["ES2021", "dom"],
		"forceConsistentCasingInFileNames": true,
		"module": "ESNext",
		"isolatedModules": true,
		"noEmit": true
	},
	"include": ["./**/*"],
	"exclude": ["./_generated"]
}
